// ZATCA Phase 2 TLV builder. MVP returns null (not yet wired).
// Phase 3 will produce real TLV-encoded base64 QR data.

import type { Invoice, Seller } from "../types";

export function generateZatcaTLV(_invoice: Invoice, _seller: Seller): string | null {
  return null;
}
